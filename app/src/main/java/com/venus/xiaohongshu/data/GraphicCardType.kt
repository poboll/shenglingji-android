// 定义包名，标识数据模型类的命名空间
package com.venus.xiaohongshu.data

/**
 * 图形卡片类型枚举类
 * 用于定义UI中显示的卡片类型
 * 区分不同媒体内容的展示方式
 */
enum class GraphicCardType {
    // 图片卡片类型，用于展示图片内容的帖子
    Graphic,
    // 视频卡片类型，用于展示视频内容的帖子
    Video
}